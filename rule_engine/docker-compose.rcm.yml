# Production Docker Compose for RCM application
#
# Domain: set RCM_DOMAIN in .env (e.g. testing.rcm.caplineservices.in).
# SSL: Obtain cert first so nginx can start, e.g.:
#   certbot certonly --standalone -d $RCM_DOMAIN
#   (stop anything on 80/443 first). Then start compose.
# Optional: use ENV=prod-nossl and no letsencrypt volume to run without SSL (e.g. behind another proxy).
# If running alongside RuleEngine on same host, change frontend ports (e.g. 8080:80, 8443:443).
#
# Run:   docker compose -f docker-compose.rcm.yml up --build -d
# Stop:  docker compose -f docker-compose.rcm.yml down

services:
  # Spring Boot Backend (RCM)
  rcm-backend:
    build:
      context: .
      dockerfile: rcm/Dockerfile
    container_name: rcm-backend
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"
    ports:
      - "8082:8081"  # Host 8082 → container 8081 (avoids conflict with ruleengine on 8081)
    expose:
      - "8081"  # Internal port for nginx to access via Docker network
    env_file: .env
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE:-prod}
    volumes:
      - /opt/project/tricon/files:/opt/project/tricon/files
      - /opt/project/tricon/log:/opt/project/tricon/log
      - /opt/project/attachments:/opt/project/attachments
      - /opt/project/pdf:/opt/project/pdf
      - /opt/project/xml:/opt/project/xml
    networks:
      - rcm-network
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8081/account/login > /dev/null 2>&1 || curl -sf http://localhost:8081/ > /dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

  # Angular Frontend (Nginx)
  rcm-frontend:
    build:
      context: ./rcm-client-app
      dockerfile: Dockerfile
      args:
        ENV: prod
        BUILD_CONFIG: production
    container_name: rcm-frontend
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"
    environment:
      - RCM_DOMAIN=${RCM_DOMAIN}
      - RCM_SERVER_NAME=${RCM_SERVER_NAME}
      - PROXY_CONNECT_TIMEOUT=${PROXY_CONNECT_TIMEOUT:-60s}
      - PROXY_SEND_TIMEOUT=${PROXY_SEND_TIMEOUT:-1800s}
      - PROXY_READ_TIMEOUT=${PROXY_READ_TIMEOUT:-1800s}
      - NGINX_SEND_TIMEOUT=${NGINX_SEND_TIMEOUT:-1800s}
    # No host port binding – nginx-proxy (docker-compose.proxy.yml) owns 80/443
    # and routes traffic here via the shared-proxy network.
    expose:
      - "80"
    depends_on:
      - rcm-backend
    networks:
      - rcm-network
      - shared-proxy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost > /dev/null 2>&1 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  rcm-network:
    driver: bridge
  shared-proxy:
    external: true

